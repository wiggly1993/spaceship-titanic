# spaceship-titanic

Feature engineering TODO: group-based imputation, total spending feature, group size from PassengerId, ensemble modeling (stack RF + XGBoost + LightGBM). Expected ceiling: 85-87% accuracy.